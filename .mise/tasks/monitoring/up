#!/usr/bin/env bash
#MISE description="Start monitoring stack with shared infra exporters"
#MISE alias="mu"
./scripts/generate-postgres-init.sh
docker compose -f "$COMPOSE_DB" -f "$COMPOSE_REDIS" -f "$COMPOSE_MONITORING" --profile infra --profile monitoring up -d --remove-orphans
